/**
 * Tamaño y peso únicos para todos los títulos de sección de la landing (el mismo
 * del titular del hero): así ninguna sección queda con un título más chico o más grande.
 */
export const SECTION_TITLE_CLASSES = 'text-3xl font-black leading-[1.08] tracking-tight sm:text-5xl lg:text-6xl';
