import './errorPattern.css';

/** Fondo animado "404 ERROR" en pixel-art (patrón de Uiverse adaptado). Ocupa todo su contenedor `relative`. */
export function ErrorPattern() {
  return <div className="error-pattern" aria-hidden />;
}
